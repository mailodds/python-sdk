# coding: utf-8

"""
    MailOdds Email Platform API

    MailOdds is an email platform for validation, sending, campaigns, deliverability monitoring, and analytics. The API performs multi-layer validation checks, delivers transactional and campaign email with DKIM dual signing, and tracks engagement with privacy-first analytics.  ## Authentication  All API requests require authentication using a Bearer token. Include your API key  in the Authorization header:  ``` Authorization: Bearer YOUR_API_KEY ```  API keys can be created in the MailOdds dashboard.  ## Rate Limits  Rate limits vary by plan: - Free: 10 requests/minute - Starter: 60 requests/minute   - Pro: 300 requests/minute - Business: 1000 requests/minute - Enterprise: Custom limits  ## Response Format  All responses include: - `schema_version`: API schema version (currently \"1.0\") - `request_id`: Unique request identifier for debugging  Error responses include: - `error`: Machine-readable error code - `message`: Human-readable error description  ## Webhooks  MailOdds can send webhook notifications for job completion and email delivery events. Configure webhooks in the dashboard or per-job via the `webhook_url` field.  ### Event Types  | Event | Description | |-------|-------------| | `job.completed` | Validation job finished processing | | `job.failed` | Validation job failed | | `message.queued` | Email queued for delivery | | `message.delivered` | Email delivered to recipient | | `message.bounced` | Email bounced | | `message.deferred` | Email delivery deferred | | `message.failed` | Email delivery failed | | `message.opened` | Recipient opened the email | | `message.clicked` | Recipient clicked a link |  ### Payload Format  ```json {   \"event\": \"job.completed\",   \"job\": { ... },   \"timestamp\": \"2026-01-15T10:30:00Z\" } ```  ### Webhook Signing  If a webhook secret is configured, each request includes an `X-MailOdds-Signature` header containing an HMAC-SHA256 hex digest of the request body.  **Verification pseudocode:** ``` expected = HMAC-SHA256(webhook_secret, request_body) valid = constant_time_compare(request.headers[\"X-MailOdds-Signature\"], hex(expected)) ```  The payload is serialized with compact JSON (no extra whitespace, sorted keys) before signing.  ### Headers  All webhook requests include: - `Content-Type: application/json` - `User-Agent: MailOdds-Webhook/1.0` - `X-MailOdds-Event: {event_type}` - `X-Request-Id: {uuid}` - `X-MailOdds-Signature: {hmac}` (when secret is configured)  ### Retry Policy  Failed deliveries (non-2xx response or timeout) are retried up to 3 times with exponential backoff (10s, 60s, 300s). 

    The version of the OpenAPI document: 1.0.0
    Contact: support@mailodds.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from mailodds.models.sending_domain_dns_records import SendingDomainDnsRecords
from typing import Optional, Set
from typing_extensions import Self

class SendingDomain(BaseModel):
    """
    SendingDomain
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, description="Domain UUID")
    domain: Optional[StrictStr] = Field(default=None, description="Domain name")
    domain_type: Optional[StrictStr] = Field(default=None, description="Domain type (ns_delegated)")
    status: Optional[StrictStr] = Field(default=None, description="Domain verification status")
    dkim_selector: Optional[StrictStr] = Field(default=None, description="DKIM selector (e.g. mo1)")
    dns_records: Optional[SendingDomainDnsRecords] = None
    bimi_svg_url: Optional[StrictStr] = Field(default=None, description="BIMI SVG logo URL")
    bimi_vmc_url: Optional[StrictStr] = Field(default=None, description="BIMI VMC certificate URL")
    bimi_enabled: Optional[StrictBool] = Field(default=None, description="Whether BIMI is enabled")
    forward_replies_to: Optional[StrictStr] = Field(default=None, description="Reply forwarding address")
    is_primary: Optional[StrictBool] = Field(default=None, description="Whether this is the account primary/default sending domain")
    created_at: Optional[datetime] = None
    updated_at: Optional[datetime] = None
    __properties: ClassVar[List[str]] = ["id", "domain", "domain_type", "status", "dkim_selector", "dns_records", "bimi_svg_url", "bimi_vmc_url", "bimi_enabled", "forward_replies_to", "is_primary", "created_at", "updated_at"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending_dns', 'dns_partial', 'active', 'suspended']):
            raise ValueError("must be one of enum values ('pending_dns', 'dns_partial', 'active', 'suspended')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SendingDomain from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of dns_records
        if self.dns_records:
            _dict['dns_records'] = self.dns_records.to_dict()
        # set to None if bimi_svg_url (nullable) is None
        # and model_fields_set contains the field
        if self.bimi_svg_url is None and "bimi_svg_url" in self.model_fields_set:
            _dict['bimi_svg_url'] = None

        # set to None if bimi_vmc_url (nullable) is None
        # and model_fields_set contains the field
        if self.bimi_vmc_url is None and "bimi_vmc_url" in self.model_fields_set:
            _dict['bimi_vmc_url'] = None

        # set to None if forward_replies_to (nullable) is None
        # and model_fields_set contains the field
        if self.forward_replies_to is None and "forward_replies_to" in self.model_fields_set:
            _dict['forward_replies_to'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SendingDomain from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "domain": obj.get("domain"),
            "domain_type": obj.get("domain_type"),
            "status": obj.get("status"),
            "dkim_selector": obj.get("dkim_selector"),
            "dns_records": SendingDomainDnsRecords.from_dict(obj["dns_records"]) if obj.get("dns_records") is not None else None,
            "bimi_svg_url": obj.get("bimi_svg_url"),
            "bimi_vmc_url": obj.get("bimi_vmc_url"),
            "bimi_enabled": obj.get("bimi_enabled"),
            "forward_replies_to": obj.get("forward_replies_to"),
            "is_primary": obj.get("is_primary"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at")
        })
        return _obj


